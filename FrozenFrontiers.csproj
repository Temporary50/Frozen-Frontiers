<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Release</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>

    <ProjectGuid>{E7F45D2A-8C9A-4C9F-AF32-9F1A6D41F001}</ProjectGuid>
    <OutputType>Library</OutputType>
    <RootNamespace>FrozenFrontiers</RootNamespace>
    <AssemblyName>FrozenFrontiers</AssemblyName>
    <TargetFrameworkVersion>v4.7.2</TargetFrameworkVersion>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="Assembly-CSharp">
      <HintPath>D:\SteamLibrary\steamapps\common\Slime Rancher\SlimeRancher_Data\Managed\Assembly-CSharp.dll</HintPath>
    </Reference>

    <Reference Include="UnityEngine">
      <HintPath>D:\SteamLibrary\steamapps\common\Slime Rancher\SlimeRancher_Data\Managed\UnityEngine.dll</HintPath>
    </Reference>

    <Reference Include="UnityEngine.CoreModule">
      <HintPath>D:\SteamLibrary\steamapps\common\Slime Rancher\SlimeRancher_Data\Managed\UnityEngine.CoreModule.dll</HintPath>
    </Reference>

    <Reference Include="SRML">
      <HintPath>D:\SteamLibrary\steamapps\common\Slime Rancher\Mods\SRML\SRML.dll</HintPath>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <Compile Include="FrozenFrontiers.cs" />
  </ItemGroup>

  <!-- THIS IS THE IMPORTANT PART -->
  <Target Name="Build">
    <Message Text="Building FrozenFrontiers..." Importance="high" />
    <CallTarget Targets="Compile" />
  </Target>

  <Target Name="Compile">
    <Csc
      Sources="@(Compile)"
      References="@(Reference)"
      OutputAssembly="$(OutputPath)$(AssemblyName).dll"
      TargetType="Library"
      LangVersion="$(LangVersion)" />
  </Target>

</Project>
